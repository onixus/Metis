# Итерация 16 — согласование PR #7 с main

Объединить ветку пилота и корпоративных адаптеров с main, сохранив EC-01…13, PG-01, CM-08/09, DL-04/05 и шаблоны APEX.

1. Разобрать две реализации экономики и сохранить публичные сценарии обеих сторон без ослабления ABAC, аудита и персистентности.
2. Объединить OpenAPI, приложение, compliance и интерфейс; перегенерировать производные файлы.
3. Сохранить обе истории планов и ADR, не менять применённые миграции.
4. Проверить Go race, интеграционные тесты PostgreSQL, линтеры, генерацию и web.
5. Создать merge-коммит, отправить в PR #7 и актуализировать описание с результатами проверки.

## Результат

- Две модели экономики сохранены: журнал периодов `economics`, конструктор `economics/modeling`; существующие URL не менялись, уникализирован operationId импорта конструктора.
- Сохранены APEX, маркетинг, аналитика, лицензия, прогноз delivery и новые сценарии compliance. Scoped настройки и строгие права журнала периодов сохранены.
- Исторические миграции main 3/4 сохранены в history; переход 5 поддерживает базы обеих веток без изменения их goose-истории. Два тестовых upgrade и повторный запуск прошли.
- Go `-race -tags integration ./...`, PostgreSQL хранилища и HTTP e2e прошли. Отдельный runtime-набор API/worker под metis_app и отказные финансовые политики прошли.
- Web lint/typecheck/4 теста/build прошли; остаётся предупреждение Vite о размере основного chunk. Лицензии допустимы.
- Рабочий стенд и его данные не менялись. Уязвимости проверяет существующий CI; исключение main для excelize документировано в ADR-0005 и сохранено.
