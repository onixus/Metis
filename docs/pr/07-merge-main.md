# Согласование PR #7 с main

Merge сохраняет изменения обеих веток: пилот, корпоративные коннекторы, сохраняемые версии финансовых периодов и карточки Git-продуктов; конструктор финансовых полей/метрик, маркетинг, аналитика, лицензия поставки, шаблоны APEX и расширения compliance/delivery из main.

Оба финансовых API сохранены. Конструктор перенесён в economics/modeling; его старое ограничение MemStore сохраняется. Книги периодов продолжают храниться в PostgreSQL. Автоматической синхронизации между двумя моделями нет. ActionReadModelFinance/ActionWriteModelFinance сохраняют права конструктора и не расширяют доступ к книгам. OperationId импорта конструктора стал importModelFinanceFile, URL не изменён.

Новая миграция compliance 5 поддерживает обе исторические схемы и сохраняет настройки. Исторические SQL main 3/4 лежат без изменений в db/compliance/history/main. Подробности: ADR-0009, план 16.

Проверка: все Go race + PostgreSQL integration + HTTP e2e; отдельный runtime API/worker под metis_app; два upgrade базы с сохранением данных и повторной миграцией; отказные ABAC-тесты; генерация OpenAPI/sqlc/TypeScript; web lint/typecheck/4 теста/build; golangci-lint; лицензии и gitleaks. Результат GitHub CI относится к точному head PR и проверяется отдельно.

Код согласования и новые тесты написаны Codex. Проверка человеком пока не выполнена (NF-L01). Рабочий стенд не переустанавливался.
