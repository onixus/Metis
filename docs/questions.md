# Открытые вопросы

Формат: номер, дата, вопрос, выбранный временный вариант, где помечено в коде.

| № | Дата | Вопрос | Временный вариант | Метка |
| --- | --- | --- | --- | --- |
| 01 | 2026-09-17 | Путь Go-модуля: организация и репозиторий не заданы | `github.com/onixus/metis` по владельцу локального репозитория | `go.mod` |
| 02 | 2026-09-17 | Значения по умолчанию коэффициентов критичности не согласованы с финансами (ТЗ 8.2) | Значения из ТЗ 2.4: блокирует 1,0, ускоряет 0,5, желательно 0,2; хранятся в настройках портфеля | `TODO(question-02)` в `portfoliograph` |
| 03 | 2026-09-17 | Какая CRM у заказчика | Порт CRM с файловым адаптером (CSV) на этапе 1 | `adapters/crmfile` |
| 04 | 2026-09-17 | Локальные учётные записи для аварийного доступа (AD-01): формат хранения пароля и политика | Аварийный доступ через bootstrap-токен из хранилища секретов, без парольных учёток в БД на этапе 1 | `TODO(question-04)` в `identityaccess` |
| 05 | 2026-09-17 | Docker-демон недоступен в среде агента | Интеграционные тесты с PostgreSQL помечены тегом `integration` и требуют `METIS_TEST_DATABASE_URL`; в CI поднимается сервис PostgreSQL | `//go:build integration` |
| 06 | 2026-09-17 | RM-03/PG-08: сдвигать ли автоматически даты элементов roadmap, привязанных к затронутым (зависимым) фичам, на implied_date | Даты затронутых элементов не меняются — только запись в историю с причиной «затронуто сдвигом фичи X»; новую дату подтверждает владелец продукта через ChangeDates | `TODO(question-06)` в `roadmap/handler.go` |
| 07 | 2026-09-17 | Гипотезы discovery (DS-01) — этап 2; куда привязывать сигнал до их появления | Закрыт итерацией 11: `signals.Service.LinkToHypothesis`, принадлежность гипотезы продукту проверяет `discovery.Service.LinkSignalToHypothesis` | закрыт итерацией 11 |
| 08 | 2026-09-17 | Срок разбора сигнала по умолчанию (SG-03) не задан в ТЗ | 14 дней от приёма, настройка `Service.DefaultTriageDays` | `TODO(question-08)` в `signals/service.go` |
| 09 | 2026-09-17 | Режим аутентификации стенда и e2e без Keycloak | `METIS_AUTH_MODE=hmac` с общим секретом только для стенда; в Helm по умолчанию `oidc` | `identityaccess/hmac.go` |
| 10 | 2026-09-17 | Хеш записи аудита считается до записи в БД, а `timestamptz` хранит микросекунды и `jsonb` нормализует числа в `details`; при нетипичных значениях проверка целостности может не сойтись | Часы аудита усекаются до микросекунд (`audit/pgstore.Clock`), PG-хранилище отклоняет наносекунды; рассмотреть хранение канонического JSON отдельным полем | `audit/pgstore` |
| 11 | 2026-09-17 | Правило depguard `domain-purity` покрывало `internal/kernel/**` и запрещало pgx в инфраструктурных подпакетах kernel | Маска сужена до `internal/kernel/*.go`: домен kernel чист, подпакеты `pgdb`, `outbox`, `migrate` — инфраструктура | `.golangci.yml` |
| 12 | 2026-09-17 | Хранилища PostgreSQL для signals, prioritization, roadmap, delivery | Закрыт итерацией 11: PG-хранилища всех модулей подключены в `app.Build` при `METIS_STORAGE=postgres` | `internal/app` |
| 13 | 2026-09-17 | Обработчику `roadmap.EventDatesChanged` (CT-03) нужны привязки элемента roadmap (фича, релиз), а payload `roadmap.DateChange` несёт только `ItemID`; публичного метода чтения элемента в roadmap нет | В commitments объявлен порт `RoadmapReader.ItemLinks(ctx, sc, itemID) (featureID, releaseID, error)`; без порта события roadmap пропускаются. Реализация — в roadmap следующей волной; сигнатура использует только kernel/authz, т.к. commitments импортирует roadmap и обратный импорт даст цикл | `TODO(question-13)` в `commitments/service.go` |
| 14 | 2026-09-17 | CT-04: как планировать продление, если сертификат истекает сегодня или уже истёк (конец интервала `DueDate−1` раньше `now`) | Элемент roadmap создаётся однодневным `[now, now]`; ТЗ срок не уточняет | `TODO(question-14)` в `commitments/service.go` |
| 15 | 2026-09-17 | RM-05: как сопоставлять версию релиза со строками матрицы совместимости контракта — точное совпадение строки версии или диапазоны/semver | Точное совпадение `Release.Version` с `ProviderVersion`/`ConsumerVersion` пары контракта; строки других версий не показываются | `roadmap.Service.compatibility` |
| 16 | 2026-09-17 | PR-05: где хранятся коэффициенты стоимости подтверждения изменений по классу влияния и признак `SSDLCCertified` — в prioritization или в compliance | Порт `prioritization.ImpactCost` возвращает готовую сумму; коэффициенты и учёт `SSDLCCertified` — на стороне compliance (CM-06). Без порта `confirmation_cost = 0` | `prioritization.ImpactCost` |
| 17 | 2026-09-17 | CT-04: пороги корзин Now/Next/Later для автосозданного элемента продления сертификата в ТЗ не заданы | ≤90 дней до срока — now, ≤365 — next, иначе later; аудитория internal, вид feature | `roadmap.bucketFor` |
| 18 | 2026-09-17 | RM-04: разрешать ли перевод развивающегося релиза в сертифицированную ветку, если в нём уже есть элементы вида feature | Запрещено (`ErrConflict`), пока элементы не перепривязаны; смена ветки после `released`/`eol` запрещена всегда | `roadmap.Service.UpdateRelease` |
| 22 | 2026-09-17 | CM-03/PR-05: стоимость подтверждения по классу влияния, доля скидки при сертифицированных процессах РБПО и срок действия сертификата (EOL baseline) не заданы в ТЗ | `compliance.DefaultSettings()`: none 0, analysis_required 50 000 ₽, security_functions 300 000 ₽, скидка 0,5, срок 5 лет; меняются через `UpdateSettings` (admin) | `TODO(question-15)` в `compliance/model.go` |
| 23 | 2026-09-17 | CM-06/CM-07: порт `GraphReader.Feature` (`portfoliograph.Service.Feature`) требует приватного чтения продукта, а роль compliance пишет при стратегическом доступе; оценка класса и расчёт затронутых baseline по фиче требуют приватного доступа к продукту фичи | Закрыт итерацией 11: в portfoliograph добавлен `FeatureProduct(ctx, sc, id) (kernel.ID, error)` — продукт фичи входит в стратегический срез (ТЗ 2.4), сама фича нет; порт `compliance.GraphReader` использует его вместо `Feature`, приватный доступ роли compliance не требуется | закрыт итерацией 11 |
| 19 | 2026-09-17 | DS-04: обход трассировки от узла «решение» требует обратного порта модуля decisions (связи решения → гипотезы/фичи/сигналы); в discovery объявлен только `DecisionLinks.DecisionsFor(kind, id)`. Переходы статусов гипотезы и переоткрытие confirmed/rejected → draft в ТЗ не заданы | `Trace(kind=decision)` возвращает ошибку валидации; трассировка от решения — методом модуля decisions. Переходы: draft → testing\|rejected, testing → confirmed\|rejected\|draft, confirmed\|rejected → draft (наименее необратимый вариант) | `discovery.Trace`, `discovery.allowedTransition` |
| 20 | 2026-09-17 | DA-01: кто читает и пишет портфельные решения без продукта (`ProductID = NilID`) — в ТЗ роли не заданы; `Scope.Product(NilID)` для presale даёт стратегический доступ ко «всем продуктам» | Запись — `ActionWriteDecisions` для `NilID` (cpo/admin/service); чтение — только роли cpo/admin/service, presale и PM портфельные решения не видят | `TODO(question-20)` в `decisions/service.go` |
| 21 | 2026-09-17 | DA-01/ТЗ 4.3: обновлять ли page properties (`metis_decision_status`) и тело страницы ADR при принятии и замене решения, и в каком формате хранить значения свойств (Confluence принимает любой JSON) | Страница создаётся один раз при `RequestPage`; статус на странице не синхронизируется; значения свойств — строки, нестроковые при чтении приводятся к компактному JSON | `TODO(question-21)` в `decisions/handler.go`, `adapters/confluence/confluence.go` |
| 24 | 2026-09-17 | SG-04: индекс похожести на pgvector требует модели эмбеддингов, а порт `Embedder` появится только на этапе 5 (AI-10); какую модель и размерность `vector(N)` закладывать в схему | `discovery/pgstore.PGIndex` работает на полнотекстовом поиске PostgreSQL (`tsvector`, конфигурация `russian`, `ts_rank_cd` с нормализацией в [0, 1)) с тем же интерфейсом `SimilarityIndex`; колонка `embedding vector(N)` в `discovery.embeddings` не добавлена — добавится новой миграцией при выборе модели | `discovery/pgstore/index.go`, `db/discovery/migrations/00001_discovery.sql` |
| 25 | 2026-09-17 | CM-03/PR-05: `compliance.Settings` хранятся в памяти `compliance.Service` (интерфейс `compliance.Store` методов настроек не объявляет), поэтому на PostgreSQL-стенде они теряются при перезапуске | PG-хранилище compliance настройки не персистит; расширение `compliance.Store` методами `Settings/SaveSettings` и таблица `compliance.settings` — следующей итерацией вместе с правкой `Service` | `compliance/pgstore` |
| 27 | 2026-09-17 | CM-01/CM-02: код набора требований привязан к одному типу продукта, а шаблоны треков по умолчанию (`compliance.DefaultTemplates`) ссылаются на одни коды `REESTR`/`FSTEC-UD4` для всех типов; baseline продуктов других типов создаётся без ссылки на набор | Seed заводит наборы для инфраструктурного типа (baseline Server); для остальных типов `CertifiedBaseline.RequirementSetID` пуст, пока не решено, делать ли код набора общим для типов или коды в шаблонах — по типу продукта | `TODO(question-24)` в `seed/stage2.go` |
| 28 | 2026-09-17 | CT-03: `portfoliograph.ShiftResult.Commitments` содержит только обязательства затронутых (зависимых) фич; обязательства самой сдвинутой фичи в ответ `shift-date` не попадают, хотя алерт по ним поднимается обработчиком outbox | Ответ API оставлен как есть; сценарий приёмки проверяет алерт, а не список в ответе. Уточнить, должен ли ответ включать обязательства исходной фичи | `commitments.Service.AffectedCommitments` |
| 26 | 2026-09-17 | DA-01/ТЗ 4.3: `POST /decisions/{id}/request-page` без адаптера базы знаний (пустой `METIS_CONFLUENCE_BASE_URL`) возвращает 503, чтобы событие не зависало в outbox без обработчика; пространство по умолчанию — `METIS_CONFLUENCE_SPACE` | Так; альтернатива — принимать запрос и хранить событие до подключения адаптера | `httpapi.RequestDecisionPage`, `app.knowledgeSpace` |
| 29 | 2026-09-18 | DA-01/ТЗ 4.3: обработчик `decisions.page.requested` создаёт страницу в базе знаний и отдельным шагом записывает `PageID` в БД; при падении процесса между этими шагами повтор события создаст вторую страницу ADR того же решения | Окно сужено: адаптер Confluence возвращает созданную страницу вместе с ошибкой оформления (метки, свойства), обработчик сохраняет `PageID` до возврата ошибки, поэтому повтор идёт по ветке «страница есть». Остаточный риск (сбой между вызовом базы знаний и записью в БД) снимается только идемпотентным ключом на стороне базы знаний — в Confluence Data Center REST такого ключа нет; вариант — искать страницу по метке и свойству `metis_decision_id` перед созданием | `TODO(question-29)` в `decisions/handler.go`, `adapters/confluence.Client.CreatePage` |
| 30 | 2026-09-18 | EC-08: ТЗ не задаёт ключи финансовых статей (выручка, ФОТ, прямые затраты, маркетинг, бюджет) | Ключи по умолчанию `revenue`, `payroll`, `direct_costs`, `marketing`, `budget`, `certified_revenue`, `feature_cost`, `feature_revenue`, `branch_cost`, `track_cost`; меняются настройкой `economics.Config` | `TODO(question-30)` в `economics/service.go` |
| 31 | 2026-09-18 | CM-08/CT-02: регуляторные сроки устранения уязвимости по критичности не заданы в ТЗ | critical — 30 дней, high — 60, medium — 90, low — 180 от даты сообщения; значения в `compliance.Settings.VulnerabilityFixDays`, меняются через `UpdateSettings` | `TODO(question-31)` в `compliance/model.go`, `compliance/stage3.go` |
| 32 | 2026-09-18 | DA-02: у обязательства нет плановой даты поставки, поэтому влияние сдвига capacity считается приблизительно | Обязательство считается нарушенным, если его срок попадает в окно сдвига от сегодняшней даты; точная дата появится, когда обязательство будет привязано к элементу roadmap | `TODO(question-32)` в `economics/scenario.go` |
| 33 | 2026-09-18 | DL-04: прогноз Монте-Карло требует генератора случайных чисел, gosec помечает `math/rand` как слабый | `math/rand/v2` с явным зерном: имитационная модель, а не криптография; зерно передаётся параметром для воспроизводимости прогноза | `//nolint:gosec` в `delivery/forecast.go` |
| 34 | 2026-09-18 | EC-09: привязки функций формулы задаются на уровне программы (`cel.Functions`), метод помечен deprecated, замены с той же семантикой в cel-go нет | Оставлен `cel.Functions` с `//nolint:staticcheck`; заменить, когда появится API привязок на уровне программы | `//nolint:staticcheck` в `economics/formula/engine.go` |
| 35 | 2026-09-18 | EC-01: в excelize 2.11.0 незакрытая уязвимость GO-2026-6452 — паника на специально собранной книге; исправления у поставщика нет | Паника ловится в адаптере и превращается в ошибку валидации, размер файла ограничен; обновить зависимость, когда выйдет исправление | `internal/adapters/financexlsx/financexlsx.go` |
| 36 | 2026-09-18 | DA-05: ТЗ называет Superset и ClickHouse с этапа 3; встраивание внешнего BI даёт второй контур доступа к финансовым данным | Конструктор дашбордов платформы поверх закрытого списка срезов API, аналитика остаётся в PostgreSQL (ADR-0007) | `internal/analytics` |
| 37 | 2026-09-18 | AD-06: лимит лицензии на число пользователей нечем считать — пользователи заводятся в IdP заказчика | Проверяется только лимит продуктов; лимит пользователей записан в ключе и начнёт проверяться, когда появится учёт выданных доступов (AD-02) | `internal/licensing` |
| 38 | 2026-09-18 | CM-08/CT-02: владелец автосозданного обязательства со сроком устранения уязвимости в ТЗ не задан | Владелец — `compliance`; станет настройкой, когда появится справочник ответственных | `TODO(question-38)` в `commitments/service.go` |
| 39 | 2026-09-18 | CM-09/CM-08: состав из SBOM собирается во время трека, а baseline создаётся только при прохождении гейта «сертификат»; сертифицированный трек закрыт для записи | Автосбор возвращает состав в ответе и пишет его в baseline, если тот уже создан; иначе состав записывается явным вызовом `PUT /baselines/{id}/components` после сертификации | `TODO(question-39)` в `compliance/stage3.go` |
| 40 | 2026-09-18 | Модули этапа 3 (`economics`, `analytics`, `licensing`) работают на `MemStore`, тогда как модули этапов 1–2 получили PG-хранилища (вопрос №12 закрыт) | На стенде и в тестах — память; PG-хранилище экономики (поля, формулы, строки данных, загрузки, правила) — отдельной итерацией по той же схеме, что остальные модули | `internal/economics/store.go`, `internal/app` |
